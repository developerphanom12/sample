export enum ROUTES {
  home = "/",
  login = "/login",
  sign_up = "/sign-up",
  dashboard = "/dashboard",
  inbox = "/inbox",
  master = "/master",
  settings = "/settings",
  support = "/support",
}
